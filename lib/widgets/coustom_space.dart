import 'package:flutter/material.dart';

class CoustomSpace extends StatelessWidget {
  const CoustomSpace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
